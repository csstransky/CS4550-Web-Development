Run these commands inside the root directory of the elixir-practice folder:

mix deps.get

(cd assets && npm install)

mix phx.server


Use this command to see all the routes:

mix phx.routes


Use this to debug your Elixir files:

mix tests
